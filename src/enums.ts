export enum FieldTypeEnum {
    FLOAT = "float",
    INT = "int",
    STRING = "string",
    DATE = "date",
    ENUM = "enum",
    BOOL = "bool",
    ARRAY = "array"
}

export enum ServiceNameEnum {
    DAS = "das"
}